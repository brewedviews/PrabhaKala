# MongoDB connection string (required)
MONGO_URL=mongodb://localhost:27017

# MongoDB database name (optional, defaults to "prabha_kala")
DB_NAME=prabha_kala

# Allowed CORS origin(s) for the API (optional, defaults to "*")
CORS_ORIGINS=*
